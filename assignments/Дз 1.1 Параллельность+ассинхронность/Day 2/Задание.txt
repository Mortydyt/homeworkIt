<b>День 2 — Парсинг HTML и извлечение данных</b>

🎯 <b>Цель дня</b>
Добавить функционал парсинга HTML-контента и извлечения структурированных данных.

📋 <b>Требования</b>

<b>1. Установка библиотек для парсинга</b>
```bash
pip install beautifulsoup4 lxml
```

<b>2. Класс HTMLParser</b>
Создать класс <code>HTMLParser</code> с методами:
- <code>async def parse_html(self, html: str, url: str) -> dict</code> — парсинг HTML
- <code>extract_links(self, soup: BeautifulSoup, base_url: str) -> list[str]</code> — извлечение всех ссылок
- <code>extract_text(self, soup: BeautifulSoup, selector: str = None) -> str</code> — извлечение текста
- <code>extract_metadata(self, soup: BeautifulSoup) -> dict</code> — извлечение мета-тегов (<i>title, description, keywords</i>)

<b>3. Интеграция с AsyncCrawler</b>
Расширить <code>AsyncCrawler</code>:
- Добавить метод <code>async def fetch_and_parse(self, url: str) -> dict</code>
- Метод должен возвращать словарь с полями:
  - <code>url</code> — исходный URL
  - <code>title</code> — заголовок страницы
  - <code>text</code> — основной текст
  - <code>links</code> — список ссылок (<i>абсолютные URL</i>)
  - <code>metadata</code> — метаданные

<b>4. Обработка относительных ссылок</b>
- 🔄 Конвертировать относительные ссылки в абсолютные
- 🔍 Фильтровать внешние ссылки (<i>опционально</i>)
- ✅ Валидировать URL перед добавлением

<b>5. Извлечение специфичных данных</b>
Реализовать методы для извлечения:
- 🖼️ Всех изображений с атрибутами <code>src</code> и <code>alt</code>
- 📰 Всех заголовков (<i>h1, h2, h3</i>)
- 📊 Таблиц с данными
- 📝 Списков (<i>ul, ol</i>)

<b>6. Обработка ошибок парсинга</b>
- ⚠️ Обрабатывать некорректный HTML
- 📝 Логировать предупреждения при парсинге
- 🔄 Возвращать частичные результаты при ошибках

<b>7. Демонстрация</b>
Создать скрипт, который:
- 📥 Загружает и парсит несколько страниц
- 🔗 Извлекает заголовки и ссылки
- 💾 Сохраняет результаты в JSON
- 📊 Показывает статистику (<i>количество ссылок, длина текста</i>)

<b>Пример вывода:</b>
```python
{
    "url": "https://example.com",
    "title": "Example Domain",
    "text_length": 1234,
    "links_count": 5,
    "links": ["https://example.com/page1", ...],
    "images_count": 2
}
```

<b>8. Тестирование</b>
- ✅ Протестировать парсинг валидного HTML
- ⚠️ Протестировать обработку битого HTML
- 🔗 Протестировать извлечение ссылок
- 🔄 Проверить конвертацию относительных URL

<b>🎉 Критерии успеха</b>
- ✅ HTML корректно парсится
- 📊 Данные извлекаются правильно
- 🔄 Относительные ссылки конвертируются в абсолютные
- 🛡️ Ошибки парсинга не ломают программу

---
<i>Удачи с реализацией!</i> 🚀
