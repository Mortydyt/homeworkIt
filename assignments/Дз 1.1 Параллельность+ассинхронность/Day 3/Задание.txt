<b>День 3 — Управление конкурентностью и очередями</b>

🎯 <b>Цель дня</b>
Реализовать систему управления конкурентностью, очередей задач и контроля загрузки.

📋 <b>Требования</b>

<b>1. Класс CrawlerQueue</b>
Создать класс для управления очередью URL:
- <code>add_url(self, url: str, priority: int = 0)</code> — добавление URL с приоритетом
- <code>async def get_next(self) -> str | None</code> — получение следующего URL
- <code>mark_processed(self, url: str)</code> — пометка URL как обработанного
- <code>mark_failed(self, url: str, error: str)</code> — пометка URL как неудачного
- <code>get_stats(self) -> dict</code> — статистика очереди

<b>2. Класс SemaphoreManager</b>
Реализовать управление семафорами:
- 🚦 Ограничение количества одновременных запросов к одному домену
- ⚡ Глобальное ограничение конкурентности
- 📊 Отслеживание активных задач

<b>3. Расширенный AsyncCrawler</b>
Добавить в <code>AsyncCrawler</code>:
- <code>async def crawl(self, start_urls: list[str], max_pages: int = 100)</code> — основной метод краулинга
- 📋 Использование очереди для управления URL
- ➕ Автоматическое добавление найденных ссылок в очередь
- 📏 Ограничение глубины обхода (<code>max_depth</code>)
- 🔍 Фильтрация уже посещённых URL

<b>4. Управление состоянием</b>
Реализовать:
- <code>visited_urls: set</code> — множество посещённых URL
- <code>failed_urls: dict</code> — словарь неудачных URL с ошибками
- <code>processed_urls: dict</code> — словарь обработанных URL с результатами

<b>5. Контроль глубины обхода</b>
- 📏 Параметр <code>max_depth</code> для ограничения глубины
- 📊 Отслеживание текущей глубины для каждого URL
- ⏭️ Пропуск ссылок, превышающих максимальную глубину

<b>6. Фильтрация URL</b>
Реализовать фильтры:
- 🏠 Только тот же домен (<code>same_domain_only</code>)
- ❌ Исключение определённых паттернов (<code>exclude_patterns</code>)
- ✅ Включение только определённых паттернов (<code>include_patterns</code>)

<b>7. Прогресс и статистика</b>
Выводить в реальном времени:
- 📄 Количество обработанных страниц
- ⏳ Количество в очереди
- ❌ Количество ошибок
- ⚡ Скорость обработки (<i>страниц/сек</i>)

<b>8. Демонстрация</b>
Создать скрипт, который:
- 📍 Начинает с одного или нескольких стартовых URL
- 🌐 Обходит сайт с ограничением глубины
- 📊 Показывает прогресс в реальном времени
- 💾 Сохраняет все найденные данные

<b>Пример использования:</b>
```python
async def main():
    crawler = AsyncCrawler(max_concurrent=10, max_depth=2)
    results = await crawler.crawl(
        start_urls=["https://example.com"],
        max_pages=50,
        same_domain_only=True
    )
    print(f"Обработано: {len(results)} страниц")

asyncio.run(main())
```

<b>9. Тестирование</b>
- ✅ Протестировать очередь с приоритетами
- 📏 Протестировать ограничение глубины
- 🔍 Протестировать фильтрацию URL
- 🚫 Проверить отсутствие дубликатов в <code>visited_urls</code>

<b>🎉 Критерии успеха</b>
- ✅ Очередь корректно управляет URL
- 🚦 Конкурентность контролируется семафорами
- 📏 Глубина обхода ограничивается
- 🚫 Дубликаты не обрабатываются повторно
- 📊 Статистика отображается корректно

---
<i>Удачи с реализацией!</i> 🚀
