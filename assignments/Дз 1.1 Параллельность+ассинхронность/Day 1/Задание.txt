<b>День 1 — Базовый асинхронный HTTP-клиент</b>

🎯 <b>Цель дня</b>
Создать базовый асинхронный HTTP-клиент для загрузки веб-страниц с использованием <code>aiohttp</code>.

📋 <b>Требования</b>

<b>1. Установка зависимостей</b>
Установить необходимые библиотеки:
```bash
pip install aiohttp aiofiles
```

<b>2. Базовый класс AsyncCrawler</b>
Создать класс <code>AsyncCrawler</code> с методами:
- <code>__init__(self, max_concurrent: int = 10)</code> — инициализация с ограничением конкурентности
- <code>async def fetch_url(self, url: str) -> str</code> — загрузка одной страницы
- <code>async def fetch_urls(self, urls: list[str]) -> dict[str, str]</code> — параллельная загрузка списка URL
- <code>async def close(self)</code> — закрытие сессии

<b>3. Использование aiohttp.ClientSession</b>
- 🌐 Создать сессию с таймаутами (<i>connect, read</i>)
- 🔗 Использовать connection pooling
- ✅ Правильно закрывать сессию после использования

<b>4. Обработка ошибок</b>
Обрабатывать следующие исключения:
- ❌ <code>aiohttp.ClientError</code> — сетевые ошибки
- ⏰ <code>asyncio.TimeoutError</code> — таймауты
- 🚫 <code>aiohttp.ClientResponseError</code> — HTTP ошибки (<i>404, 500 и т.д.</i>)

<b>5. Базовое логирование</b>
Логировать:
- ▶️ начало загрузки URL
- ✅ успешное завершение
- ⚠️ ошибки с указанием URL и типа ошибки

<b>6. Демонстрация</b>
Создать скрипт, который:
- 📥 загружает <i>5-10</i> различных URL параллельно
- 📊 выводит статус каждого запроса
- ⏱️ измеряет общее время выполнения
- 🚀 показывает преимущество параллельной загрузки

<b>Пример использования:</b>
```python
async def main():
    crawler = AsyncCrawler(max_concurrent=5)
    urls = [
        "https://example.com",
        "https://httpbin.org/delay/1",
        "https://httpbin.org/delay/2",
    ]
    results = await crawler.fetch_urls(urls)
    await crawler.close()
    print(f"Загружено {len(results)} страниц")

asyncio.run(main())
```

<b>7. Тестирование</b>
- ✅ Протестировать загрузку валидных URL
- ❌ Протестировать обработку несуществующих URL
- ⏰ Протестировать таймауты
- ⚡ Сравнить время последовательной и параллельной загрузки

<b>🎉 Критерии успеха</b>
- ✅ Класс корректно загружает страницы асинхронно
- 🛡️ Ошибки обрабатываются без падения программы
- 📝 Логирование работает
- ⚡ Параллельная загрузка быстрее последовательной

---
<i>Удачи с реализацией!</i> 🚀
